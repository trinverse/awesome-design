# NPM Configuration for Artifactory
# Uncomment and configure the following lines for your Artifactory setup

# registry=https://your-artifactory-url/artifactory/api/npm/npm-virtual/
# always-auth=true
# _auth=YOUR_BASE64_ENCODED_CREDENTIALS

# For scoped packages (e.g., @angular)
# @angular:registry=https://your-artifactory-url/artifactory/api/npm/npm-virtual/

# Proxy configuration (if needed)
# proxy=http://your-proxy:8080
# https-proxy=http://your-proxy:8080
# no-proxy=localhost,127.0.0.1

# SSL Configuration (if using self-signed certificates)
# strict-ssl=false

# Cache configuration
cache-min=3600